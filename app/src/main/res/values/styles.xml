<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:colorBackground">@color/chocolate_brown_700</item>
        <item name="toolbarStyle">@style/ThemeOverlay.Sarviun.Toolbar</item>

    </style>

    <style name="ThemeOverlay.Sarviun.BottomSheet" parent="">
        <item name="android:textColorSecondary">@color/chocolate_brown_700</item>
        <item name="android:textColorTertiary">@color/chocolate_brown_700</item>
        <item name="android:indeterminateTint">@color/chocolate_brown_700</item>
    </style>

    <style name="ThemeOverlay.Sarviun.Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="titleTextColor">@color/chocolate_brown_700</item>
    </style>
    
    <style name="BottomSheetButtonStyle" parent="@style/Widget.AppCompat.Button.Borderless.Colored">
        <item name="drawableTint">@color/colorComplementary</item>
        <item name="android:textColor">@color/colorComplementary</item>
    </style>


</resources>
